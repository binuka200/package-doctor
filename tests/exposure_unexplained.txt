# Category entries that predate per-entry evidence and carry no `why`.
#
# This file only shrinks. A test asserts that every categorised package in
# exposure.toml either records a reason or is listed here, so a new entry
# without one fails CI. Remove a name from this list when its reason is
# written - and never add one.
#
# It is empty: as of schema_version 6 every entry in the map records what
# convinced its curator.
